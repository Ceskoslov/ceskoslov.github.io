+++
title = "在这里写草稿"
date = 2026-08-14
description = "这篇文章只会在使用 --drafts 本地预览时出现。"
draft = true

[taxonomies]
tags = ["草稿"]
+++

这是一个草稿模板。因为设置了 `draft = true`，正式构建时不会发布。

## 小标题

从这里开始写正文。发布前将 `draft` 改为 `false` 或删除这一行。

