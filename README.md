# ceskoslov.github.io

A static personal site generated by [Zola](https://www.getzola.org/).

## What's Inside

- Zola and Tera templates, built with a single binary
- Markdown posts with TOML front matter
- Home, post, archive, tags, about, and 404 pages
- Atom feed, sitemap, robots.txt, and social sharing metadata
- Responsive navigation, dark and light themes, print styles, and reduced-motion support
- GitHub Actions for automatic builds and deployments to Pages
- No third-party fonts or front-end JavaScript frameworks

## Installing Zola

```bash
cargo install --locked --git https://github.com/getzola/zola --tag v0.23.3
```

Alternatively, download a pre-built binary from [Zola 0.23.3](https://github.com/getzola/zola/releases/tag/v0.23.3), matching CI.

To verify the installation:

```bash
zola --version
```

## Local Preview

```bash
zola serve
```

Open `http://127.0.0.1:1111` in your browser. The page will automatically refresh when you save changes to Markdown, templates, or CSS.

To preview drafts together:

```bash
zola serve --drafts
```

To check and build the site:

```bash
zola check --skip-external-links
zola build
```

The generated site is located in `public/`, which is ignored by Git.

## Writing a Post

Create a Markdown file in `content/writing/`. Use a date followed by a descriptive, lowercase English slug, for example `2026-08-14-my-new-post.md`:

````markdown
+++
title = "Title"
date = 2026-08-14
description = "A short description that appears in search results and on the homepage."

[taxonomies]
tags = ["Tech", "Notes"]
+++

Begin writing your post here.

## Secondary Title

Markdown, tables, and syntax-highlighted code are supported.

```rust
fn main() {
    println!("hello, chaos");
}
```
````

Posts are sorted by the front matter `date`; the filename date is a naming convention.
Include `draft = true` in the front matter to mark it as a draft.

### Inserting Images

Place images in `static/assets/images/posts/` and use them in your posts:

```markdown
![Image description](/assets/images/posts/example.jpg)
```

## Deploying to GitHub Pages

The repository already contains `.github/workflows/pages.yml`. To deploy:

1. Open the GitHub repository's **Settings → Pages**.
2. Set **Build and deployment → Source** to **GitHub Actions**.
3. Commit and push to the `main` branch.

Actions will install Zola, build, and deploy `public/`. The site URL is `https://ceskoslov.github.io` when the repository name is `ceskoslov.github.io`.

If the default branch is not `main`, modify `branches` in the workflow. If you change the GitHub username, update `base_url`, `author`, and `github_username` in `zola.toml`.

## Customization

Site information is in `zola.toml`, the about page is `content/about.md`, and theme variables are at the top of `static/assets/css/style.css`:

```css
:root {
  --paper: #eef0ec;
  --ink: #111718;
  --accent: #b84435;
  --terrain-low: #26363a;
  --terrain-high: #e4e8e3;
  --terrain-line: #52696f;
  --survey-accent: #b84435;
}
```

## Directory Structure

```text
.
├── .github/workflows/   # Automatic GitHub Pages deployment
├── content/
│   ├── writing/         # Markdown posts and drafts
│   ├── about.md         # About page
│   └── archive.md       # Archive page
├── static/              # CSS, JavaScript, and images
├── templates/           # Tera page templates
└── zola.toml            # Zola site configuration
```

### Reading metadata

Use `updated = 2026-09-21` in front matter when revising an article. Set
`language = "zh-CN"` under `[extra]` for Chinese articles (the default is English).
Prefer language metadata to topic tags:

```toml
[taxonomies]
tags = ["Minds"]

[extra]
language = "zh-CN" # use "en" for English
```

Cards and article headers show a separate language badge. The language also controls
HTML `lang` for articles and card titles. Existing `CN`/`EN` tags remain a fallback
when `extra.language` is absent; explicit metadata takes precedence. Those legacy
labels are omitted from card/article topic lists, while their tag archive URLs remain
available. You can remove the legacy tags after setting `extra.language`.

Articles with at least three top-level headings automatically receive a collapsible
table of contents. Keep descriptions meaningful, or omit them.

### Homepage and sharing

`extra.profile_intro` controls the short homepage introduction independently of About.
`extra.featured_posts` lists content paths for selected writing; remove entries to hide them.
Selected Writing and Latest Posts share the same article cards; selections retain the
configured order and can also appear in the chronological feed.
The homepage shows the latest six posts; `/writing/` and `/archive/` contain the full list.
`extra.social_image` provides the default sharing image. An article can override it with
`social_image` and `social_image_alt` under `[extra]` (paths relative to `static/`).
The default PNG is rendered from `static/assets/images/social-card.svg`.

### Rendering and checks

The site remains a Zola build with no front-end framework. `main.js` owns navigation,
theme, tables, and the quiet-reading preference; `atlas.js` owns decorative rendering.
Quiet reading persists across pages and skips terrain initialization on the next load.
WebGL rendering is capped at 1.5 DPR and about two million pixels per canvas. Without
WebGL, a static CPU-rendered field uses at most about 180,000 pixels; it only redraws
on resize or palette changes. Reduced motion disables interaction and reveal motion.

Run the publishing regression checks with Python 3 and Zola:

```bash
python3 tests/site_regression.py
```

Browser checks require Node.js 22 and development dependencies (not needed to build
or publish the site):

```bash
npm ci
npx playwright install chromium
npm test
```

To use an existing Chromium installation, set `PLAYWRIGHT_CHROMIUM_EXECUTABLE`.
Tests build disposable sites outside the repository, including fixture articles for
archive ordering, draft exclusion, metadata, wide tables, and renderer failure modes.
PRs run these checks in GitHub Actions. Deployment depends on the same reusable
checks workflow, so both publishing and browser checks must pass before deployment.

External link checks are separate: run `zola check` manually. LinkedIn may return
HTTP 999 to automated requests; check that profile in a browser instead of treating
that response as proof of a broken link. External service availability does not block
local content validation or deployment.
